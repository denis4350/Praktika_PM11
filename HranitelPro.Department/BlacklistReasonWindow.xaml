<Window x:Class="HranitelPro.Department.BlacklistReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление в чёрный список" 
        Height="368" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="⚠️ Добавление посетителя в чёрный список" 
                   FontSize="16" FontWeight="Bold" Foreground="Red" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#F5F5F5" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Посетитель:" FontWeight="Bold"/>
                <TextBlock x:Name="VisitorNameText" Margin="0,5,0,0"/>
                <TextBlock Text="Паспорт:" FontWeight="Bold" Margin="0,10,0,0"/>
                <TextBlock x:Name="PassportText" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="Причина добавления:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="ReasonBox" Grid.Row="2" Height="120" TextWrapping="Wrap" 
                 AcceptsReturn="True" Margin="0,25,0,0" VerticalAlignment="Top"
                 BorderBrush="#DDD" BorderThickness="1" Padding="5"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
            <Button Content="✅ ДОБАВИТЬ" Click="Add_Click" 
                    Width="120" Height="35" Background="#4CAF50" Foreground="White" Margin="5"/>
            <Button Content="ОТМЕНА" Click="Cancel_Click" 
                    Width="100" Height="35" Background="#9E9E9E" Foreground="White" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>